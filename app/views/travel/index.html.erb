<div class="container">
  <div class="row">
    <div class="col-lg-12 mt-5">
      <div class="mx-auto mt-5" style="width: 400px">
        <%= form_with(url: search_path, method: 'get', local: true) do %>
          <div class="form-group">
            <%= label_tag :country, 'Search for a country '%>
            <%= text_field_tag :country, nil, placeholder: 'Eg. Germany', class: 'form-control' %>
          </div>
          <%= button_tag 'Search', class: 'btn btn-success btn-block' %>
        <% end %>
        <!-- <%#= form_with(url: search_path, method: 'get', local: true) do %>
          <div class="form-group">
            <%#= label_tag :hostname, 'Search host information '%>
            <%#= text_field_tag :hostname, nil, placeholder: 'Eg. webcontrive.com', class: 'form-control' %>
          </div>
          <%#= button_tag 'Search', class: 'btn btn-success btn-block' %>
        <%# end %> -->
        <%= link_to "hacker news", stories_path %>
      </div>
    </div>
  </div>
</div>
